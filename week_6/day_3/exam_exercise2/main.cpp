#include <iostream>
#include <map>
#include <fstream>
#include <sstream>

std::map<std::string, int> readFile(std::string fileName);
int main()
{
    std::cout << "Hello, World!" << std::endl;
    return 0;
}
std::map<std::string, int> readFile(std::string fileName)
{
    std::ifstream
}